import styled from 'styled-components';

const StyledHeader = styled.header`
  text-align: center;
  border: 1px solid red;
`;

export default () => (
    <StyledHeader>
        <h1>Articles</h1>
    </StyledHeader>
);
