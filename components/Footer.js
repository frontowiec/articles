import styled from 'styled-components';

const StyledFooter = styled.footer`
  text-align: center;
  border: 1px solid red;
`;

export default () => (
    <StyledFooter>
        <h4>Footer</h4>
    </StyledFooter>
);
